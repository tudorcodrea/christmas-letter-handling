server:
  port: 8082

spring:
  cloud:
    aws:
      credentials:
        access-key: ${AWS_ACCESS_KEY}
        secret-key: ${AWS_SECRET_KEY}
      sqs:
        region: ${AWS_SQS_REGION}

letter-processor:
    aws:
      sqs:
        queue-url: ${AWS_SQS_QUEUE_URL}
